# Unreleased

- Add `CommunityToolkit.Mvvm`-based observable settings support through `SubSettings`.
- Add opt-in `AutoSave`, debounce pending tracking, cancellation, and timeout-aware debounce wait support.
- Add `ISavable` and panic-save registration before forced crash exit.
- Add application runtime duration and birthday helper APIs.
- Add xUnit v3 test project covering `net8.0` and `net10.0`.

# v0.1.0 (/04/2026)

- Initial release
